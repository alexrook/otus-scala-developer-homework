<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} %highlight(%-7([%level])) %cyan(%-25logger{25}) - %msg :: mdc[%mdc{observation}, %mdc{queue}]%n</pattern>
        </encoder>
    </appender>

    <logger name="test" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
